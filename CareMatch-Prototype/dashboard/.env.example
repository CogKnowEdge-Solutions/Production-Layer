# Copy this file to ".env" (same folder) if you need to point the
# dashboard at a different API address. Defaults to http://localhost:8000
# if this file/variable isn't present at all.
VITE_API_BASE_URL=http://localhost:8000
